# Comma-separated list of backend base URLs. The app round-robins across
# these for every /train submission.
#
# Local dev (single backend):
VITE_API_URLS=http://localhost:8000
#
# Production (both live nodes):
# VITE_API_URLS=https://your-app.onrender.com,https://your-app.up.railway.app
